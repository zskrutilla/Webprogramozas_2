# Session10 – 10. hét (MySQLi + OOP integráció)

Futtatás:
- A Session10 mappában: `php -S localhost:8000`
- Böngésző: `http://localhost:8000/Task01/` … `Task05/`

Előkészítés:
- Minden Task mappában:
  - futtassa le a `schema.sql` fájlt
  - `config.sample.php` → másolja `config.php` néven és állítsa be
